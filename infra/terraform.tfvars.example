# Copy to terraform.tfvars (gitignored) if you need to override a default.
# The site serves only static content, so there are no secrets to supply.

# Optional overrides (defaults shown):
# project_id  = "a-dougs-life"
# region      = "us-central1"
# github_repo = "dougrosa0/a-dougs-life"
